# reviewed/ 审核与治理记录

本目录用于保存正式审核痕迹，而不是原文资产本身。

## 子目录
- `ingestion-audit/`：入库审核记录
- `downgrade-log/`：降级、限用、下线记录
- `review-queue/`：待审核任务单、待补证清单

## 审核要求
- 任一语料进入 `active` 或 `limited` 前，应至少留下 1 条审核记录。
- 任一规则或片段升格为正式资产，应可追溯到审核结论。
- 发现侵权、敏感、错标、时效失效等问题时，先记治理记录，再调整状态。
